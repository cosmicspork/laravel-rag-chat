; Port
listen = 9000

; Process Manager settings for better performance and resource management.
pm = dynamic
pm.max_children = 10
pm.start_servers = 4
pm.min_spare_servers = 2
pm.max_spare_servers = 6

; Terminate requests that exceed the defined timeout.
request_terminate_timeout = 30s
