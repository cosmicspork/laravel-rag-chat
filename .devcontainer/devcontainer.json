{
    "name": "laravel-rag-chat",
    "dockerComposeFile": "docker-compose.yml",
    "service": "app",
    "forwardPorts": [
        8000,
        5173
    ],
    "postCreateCommand": "composer install && npm install && php artisan key:generate && php artisan migrate",
    "remoteUser": "vscode",
    "workspaceFolder": "/workspace"
}
